# For the CEO

> §1 executive summary. Zero code terms, zero raw scores. Every bullet ends in what it is worth —
> time, money, risk, trust. Delete this block on use.

**`<One-line takeaway.>`**

| The problem | Status today | What this delivers | What it costs |
|---|---|---|---|
| `<…>` | `<…>` | `<…>` | `<…>` |

## What we would fix

Quick wins first, then worst to least.

* `<Bullet — ends in what it is worth.>`
* `<≤10 total.>`

## The picture

**Severity scoreboard** · **top-3 quick-win cards** · **impact × effort quadrant** with every pain
point placed by ID, quick-win zone highlighted.

## What needs a CTO call

`REVERSIBLE` gates carry a default and execute on silence. `ONE-WAY` gates — schema, contract,
anything hard to undo — are never defaulted and wait for [For the CTO](for-the-cto.md).

| Gate | Question | Type | Recommendation | Evidence |
|---|---|---|---|---|
| G1 | `<…>` | `REVERSIBLE` | `<…>` | `<pointer>` |
| G2 | `<…>` | `ONE-WAY` | `<…>` | `<pointer>` |
