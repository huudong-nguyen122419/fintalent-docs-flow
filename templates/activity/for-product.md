# For product

> Scenario confirmation (§2), user journeys and UX copy (§5), pain point register (§6).
> Delete this block on use.

**`<One-line takeaway.>`**

## Scenarios — confirmed by default

Expansion proceeds after 48h or on explicit go. Veto trims the list.

1. `<one line each, default 8>`

## Journeys

Cockburn format, stage tables, coverage blocks. Verbatim current UI copy lives here — nowhere else.
Flow diagram per scenario; full sequence diagram only for the top three by RPN.

### `<Scenario name>`

| Stage | User does | System does | Copy shown | Gap |
|---|---|---|---|---|
| `<…>` | `<…>` | `<…>` | `"<verbatim>"` | `<pain ID or —>` |

## Pain point register (§6)

`QUICK WIN` first, then RPN descending.

| ID | Journey moment → what they saw → what really happened | Class | RPN | Effort | Reach × Impact × Confidence | Evidence |
|---|---|---|---|---|---|---|
| `P1` | `<…>` | `BLOCKER` | `<n>` | `<1–5>` | `<RICE>` | `<file, both repos>` |

Class thresholds: `BLOCKER` ≥60 · `CRITICAL` 36–59 · `MAJOR` 16–35 · `MINOR` <16.
`QUICK WIN` when severity ≥4, effort ≤2, confidence high.
