# For developers

> §9 fix cards. Junior test: a mid-level dev picks one up with no further reading.
> Ordered by RPN, then lowest effort. Quick wins ship in pass 1 — the rest in pass 2.
> Delete this block on use.

## `QW-1` `<title>`

**Pain** `<P#>` · **Class** `<BLOCKER | CRITICAL | MAJOR | MINOR>` · **RPN** `<n>` · **Effort** `<1–5, ≈ days>` · `QUICK WIN`

**Why it matters** `<PM sentence, RICE framing — one sentence.>`
**What changes** `<engineering sentence — one sentence.>`

**Files**

```
<repo>: <path>
<repo>: <path>
```

**Steps** — five at most

1. `<…>`
2. `<…>`

**Acceptance**

```gherkin
Given <precondition>
When <action>
Then <observable result>
```

**Watch out for** `<the trap that turns an hour into a day.>`

---

## `FIX-2` `<title>`

`<Same shape. Repeat per card.>`
