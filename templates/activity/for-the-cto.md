# For the CTO

> §3 change briefs — one block per side. Change contracts, never implementations.
> JSON samples and proposed copy live here only. Pass 2. Delete this block on use.

**`<One-line takeaway.>`**

## Gates needing you

`NEEDS CTO` first, and impossible to miss.

| Gate | Status | Question | Options | Recommendation |
|---|---|---|---|---|
| G2 | **`NEEDS CTO`** · `ONE-WAY` | `<…>` | A `<…>` · B `<…>` | `<…>` + why |
| G1 | `DECIDED` | `<…>` | — | `<what, when, by whom>` |

---

## Backend

**Where it stands** `<three sentences maximum, plain language>`

| # | Change | Type | Traces to | Notes |
|---|---|---|---|---|
| B1 | `<data or API change>` | `ADD` / `CHANGE` / `REMOVE` | `P3` | `<…>` |

```json
{ }
```

**Done when** — verification only, not implementation steps

* [ ] `<observable end state>`

---

## Frontend

**Where it stands** `<three sentences maximum, plain language>`

| # | Change | Type | Traces to | Proposed copy |
|---|---|---|---|---|
| F1 | `<screen or copy change>` | `ADD` / `CHANGE` / `REMOVE` | `P1` | `"<verbatim>"` |

**Done when**

* [ ] `<observable end state>`
* [ ] Every change above traces to a pain ID
* [ ] Every `ONE-WAY` gate decided and recorded
