# Decisions — everyone

> `README.md` of an activity folder. The only page every stakeholder opens.
> Holds §0 (decision strip) + the verdict banner. Delete this block on use.

**Activity** `<ID>` — `<activity name, verbatim from the matrix>`
**Stage** `<Revenue Stage>` · **Owner** short term `<roles>` · mid term `<roles>`
**Status** `Draft` · **Pass** `1` · **Last verified** `<YYYY-MM-DD>` on `<repo@branch>`

# `<Flow name>` — `<what it delivers, one line>`

`<Verdict banner — one line, ≤12 words.>`

## Decide in 15 seconds

**Silence = the recommended default executes after 48h.** Nobody composes an answer — you only veto.

| Who | The one question | Recommended default | If you do nothing (48h) |
|---|---|---|---|
| CEO | `<yes/no>` | `<default>` | `<consequence>` |
| CPO | `<yes/no>` | `<default>` | `<consequence>` |
| COO | `<yes/no — answered by the symptom check in "For operations">` | `<default>` | `<consequence>` |
| CTO | `<yes/no>` | `<default>` | `<consequence>` |
| Tech lead | `<yes/no — each quick win links to its card in "For developers">` | `<default>` | `<consequence>` |

## Your page

| You are | Read | It answers |
|---|---|---|
| CEO | [For the CEO](for-the-ceo.md) | What it costs, what it is worth |
| COO / Operations | [For operations](for-operations.md) | What users report, what the workaround is |
| CPO / Product | [For product](for-product.md) | Which scenarios, which copy, what hurts most |
| CTO | [For the CTO](for-the-cto.md) | What changes on each side, which gates need you |
| Tech team | [For the tech team](for-the-tech-team.md) | Surfaces, contracts, gaps, open questions |
| Developers | [For developers](for-developers.md) | Files, steps, acceptance — pick up today |
